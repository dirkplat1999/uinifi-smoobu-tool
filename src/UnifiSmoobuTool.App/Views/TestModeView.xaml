<UserControl x:Class="UnifiSmoobuTool.App.Views.TestModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" Content="Enable test mode" IsChecked="{Binding TestModeEnabled}" Margin="0,0,0,8"
                  Command="{Binding ToggleTestModeCommand}"/>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,8" Opacity="0.8"
                   Text="When enabled, automated messages and access provisioning only run for reservations that match one of the rules below - real guests are left untouched."/>
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,0,0,8" TextWrapping="Wrap" Opacity="0.8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <ComboBox ItemsSource="{Binding RuleTypeOptions}" SelectedItem="{Binding NewRuleType}" Width="120" Margin="0,0,8,0"/>
            <TextBox Text="{Binding NewRuleValue, UpdateSourceTrigger=PropertyChanged}" Width="240" Margin="0,0,8,0"/>
            <Button Content="Add rule" Command="{Binding AddRuleCommand}" Padding="10,4"/>
        </StackPanel>

        <DataGrid Grid.Row="4" ItemsSource="{Binding Rules}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove"
                                    Command="{Binding DataContext.DeleteRuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" Padding="6,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
