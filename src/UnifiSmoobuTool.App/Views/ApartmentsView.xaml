<UserControl x:Class="UnifiSmoobuTool.App.Views.ApartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Refresh from Smoobu" Command="{Binding RefreshFromSmoobuCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Load UniFi Access doors" Command="{Binding LoadUnifiResourcesCommand}" Padding="10,4"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="0,0,0,8" TextWrapping="Wrap" Opacity="0.8"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Apartments}" SelectedItem="{Binding SelectedApartment}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Smoobu Apartment" Binding="{Binding ApartmentName}" Width="*"/>
                    <DataGridTextColumn Header="Assigned UniFi doors" Binding="{Binding AssignedResourcesSummary}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="2">
                <TextBlock Text="{Binding SelectedApartment.ApartmentName, StringFormat='Doors for: {0}'}" FontWeight="Bold" Margin="0,0,0,8"/>
                <ListBox ItemsSource="{Binding AvailableResources}" Height="420" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Resource.Name}" Margin="2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Content="Save Mapping" Command="{Binding SaveMappingCommand}" Padding="10,4" Margin="0,8,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
